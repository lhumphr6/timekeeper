"""Attendance tracking router."""
from fastapi import APIRouter

router = APIRouter(prefix="/attendance", tags=["Attendance"])

# Attendance routes would be defined here
# This is a stub to satisfy the import in main.py
