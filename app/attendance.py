"""Attendance tracking router."""
from fastapi import APIRouter

router = APIRouter(prefix="/attendance", tags=["attendance"])

# Attendance endpoints would be defined here
# This is a stub to satisfy the import
